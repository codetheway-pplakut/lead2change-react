const DEVELOPMENT = {
  API: 'https://api-dev-lead2change-ctw.azurewebsites.net',
};

const PRODUCTION = {
  API: '',
};

const APP_CONFIG = {
  DEVELOPMENT,
  PRODUCTION,
};

export default APP_CONFIG;
